# intel-image-classification
Designed and evaluated convolutional neural network architectures for classification using the Intel Image Classification dataset. Compared model architectures, optimizers, learning rates, and batch sizes through 16 controlled experiments to identify the highest-performing configuration.
